<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/insert_item"
        android:title="Insert"
        android:icon="@drawable/ic_inset_button"
        app:showAsAction="always"/>

    <item android:id="@+id/invite_friend"
        android:title="Invite"
        android:icon="@drawable/ic_baseline_contact_mail_24"
        app:showAsAction="always"/>

    <item android:id="@+id/show_favorite"
        android:title="Go to favorite"
        android:icon="@drawable/ic_baseline_favorite"
        app:showAsAction="always"/>

</menu>